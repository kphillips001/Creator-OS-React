import type { GenerationRecord } from "../../generation-library/types";
import { LibraryImage } from "../../generation-library/LibraryImage";

export function CandidatePanel({ current, candidate, busy, onApprove, onRegenerate, onEdit, onReject }: { current: GenerationRecord; candidate: GenerationRecord | null; busy: boolean; onApprove: () => void; onRegenerate: () => void; onEdit: () => void; onReject: () => void }) {
  if (!candidate) return <section className="photoshoot-card photoshoot-review" aria-labelledby="photoshoot-candidate-title"><header><h2 id="photoshoot-candidate-title">Candidate Review</h2><span>Waiting for the next candidate</span></header><div className="photoshoot-candidate">No candidate generated yet.</div></section>;
  return <section className="photoshoot-card photoshoot-review" aria-labelledby="photoshoot-candidate-title"><header><h2 id="photoshoot-candidate-title">Candidate Review</h2><span>Compare before approval</span></header><div className="photoshoot-candidate-compare"><article><strong>Current Approved Shot</strong><LibraryImage record={current} /></article><article><strong>Candidate Shot</strong><LibraryImage priority record={candidate} /></article></div><div className="photoshoot-candidate-actions"><button disabled={busy} onClick={onApprove} type="button">Approve Shot</button><button disabled={busy} onClick={onRegenerate} type="button">Regenerate</button><button disabled={busy} onClick={onEdit} type="button">Edit Prompt</button><button disabled={busy} onClick={onReject} type="button">Reject Shot</button></div></section>;
}
